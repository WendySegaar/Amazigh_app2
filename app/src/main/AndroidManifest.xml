<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.amazigh_app2">
<uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application


        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".UitslagActivity"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".QuizActivity" />
        <activity
            android:name=".QuizWordActivity" />
        <activity
            android:name=".OefenenActivity"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".ScoresActivity"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".OverOnsActivity"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".CategorieActivity"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".HomeActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>